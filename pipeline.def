Bootstrap: library
From: ubuntu:latest

%post
    rm -rf pipelinex
    mkdir ./pipelinex
    apt update
    apt install python3-pip -y
    pip3 install -r requirements.txt
    


%files
    fixed_requirements.txt requirements.txt


%startscript
    ls -la

%runscript
    ls -la  