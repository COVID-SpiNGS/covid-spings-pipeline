Bootstrap: library
From: ubuntu:latest

%post
    rm -rf pipelinex
    mkdir pipelinex
    sudo apt update
    sudo apt install python3-pip
    pip3 install scipy
    


%files
    * /pipelinex/*


%startscript
    ls -la

%runscript
    ls -la  