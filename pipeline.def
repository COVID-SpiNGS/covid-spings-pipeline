Bootstrap: docker
From: continuumio/anaconda3

%post
    rm -rf pipelinex
    mkdir ./pipelinex
    apt update
    apt-get install build-essential python3.6-dev python-numpy python-matplotlib python-pysam python-htseq
    conda update -n base -c defaults conda
    conda install -c anaconda libgcc-ng
    conda init bash
    conda info | grep -i 'base environment'
    conda install --file requirements.txt -c conda-forge -c bioconda --yes
    
    

    


%files
    fixed_requirements.txt requirements.txt


%startscript
    ls -la

%runscript
    ls -la  